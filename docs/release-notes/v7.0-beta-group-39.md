# LifeOS Desktop v7.0.0-beta.1 — Group 39

## Release checkpoint

Group 39 closes LifeOS v7 at a coherent beta checkpoint. It hardens and re-proves the complete Assistant lane without adding a new capability surface.

## Complete v7 Assistant boundary

- asks retrieve only approved local LifeOS evidence sources
- ranked evidence, provenance, uncertainty, conflicts, stale data and missing data remain visible
- generated plans are review-only and `Executable: No`
- controlled review transfer creates review artifacts only
- cancelled transfer creates no artifact; duplicate confirmation remains idempotent
- memory requires explicit preview, scope, sensitivity and confirmation
- memory use is disclosed separately and cannot authorize action
- trusted current records outrank conflicting memory
- expired, revoked and deleted memory is excluded
- no tools, scripts, plugins, background work, external web search, external AI action or operational mutation exists

## Release identity

Desktop advances from `v7.0.0-alpha.4` to `v7.0.0-beta.1`. Companion remains separate and unchanged at `v0.1.0-beta.1`.

## Validation record

The Group 39 runner records exact Core/Desktop and Companion test totals, Desktop and Android Release build results, repository hygiene, NuGet vulnerability scan, full-history Gitleaks scan, `git diff --check`, synchronization and clean-tree evidence. Final manual values are recorded in `docs/manual-tests/group-39/final-validation.md` before Pack 2.

## Scope held

Website, documentation extraction, full Mobile, v8 redesign, autonomous tools and Group 40 implementation remain unstarted.
