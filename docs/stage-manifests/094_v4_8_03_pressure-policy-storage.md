# Stage 094 - v4.8 Pressure Policy Storage

Adds local JSON storage for pressure weights, score thresholds, top-signal limits, waiting-on suppression, and untrusted-review rules.

The default policy is conservative and can be reset locally.
